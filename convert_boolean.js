// Complete the method that takes a boolean value and return a "Yes" string for true, or a "No" string for false.

// We're taking in a boolean.
// We are returning a string. 
// example
// we need a conditional

const boolToWord = bool => 
    bool == true ?
     'Yes' :
      'No';